<?xml version="1.0" encoding="utf-8" ?>
<local:ContentPageBase xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Location.Core.Views"
             x:Class="Location.Core.Views.ListLocations"
             xmlns:models="clr-namespace:Locations.Core.Shared.ViewModels;assembly=Locations.Core.Shared"
             
             xmlns:system="clr-namespace:System;assembly=netstandard"             
             x:DataType="models:LocationViewModel">
    <CollectionView x:Name="cv"
     VerticalOptions="Fill"
    Header=" " 
       ItemsSource="{Binding Items}"
       SelectionMode="Single"
       SelectionChanged="CollectionView_SelectionChanged">
        <CollectionView.ItemTemplate>

            <DataTemplate x:DataType="models:LocationViewModel">
                <Grid Margin="0,10,0,0" RowSpacing="3" ColumnSpacing="3">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>

                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>

                    <Image HeightRequest="100" Grid.RowSpan="2" Grid.Column="0" VerticalOptions="Center" Grid.Row="0" Source="{Binding Photo}"></Image>

                    <Label Text="{Binding Title}" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" HorizontalOptions="Start" VerticalOptions="End" />

                    <Label Text="{Binding TimestampFormatted}" Grid.ColumnSpan="2" VerticalOptions="Start" Grid.Column="1" Grid.Row="1"/>
                    <ImageButton Source="map.svg" Pressed="ImageButton_Pressed" Grid.RowSpan="1" VerticalOptions="Center" CommandParameter="{Binding Id}" Grid.Column="3"></ImageButton>
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</local:ContentPageBase>