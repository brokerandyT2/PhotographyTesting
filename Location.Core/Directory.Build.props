<Project>
  <PropertyGroup>
    <!-- Only build Android and Windows in CI -->
    <TargetFrameworks Condition="'$(CI)' == 'true'">net9.0-android;net9.0-windows10.0.19041.0</TargetFrameworks>
  </PropertyGroup>
</Project>
